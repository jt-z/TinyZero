# TinyZero 代码分析与优化操作记录

## 一、代码分析

### 项目结构分析
- **核心代码目录**：`verl/` - 包含训练器、模型定义等核心功能
- **训练脚本**：`scripts/train_tiny_zero.sh` - PPO训练的主要入口
- **示例代码**：`examples/` - 包含各种使用示例
- **测试代码**：`tests/` - 单元测试和集成测试
- **文档**：`docs/` - 项目文档

### 最近改动分析
最近的代码改动主要集中在 `scripts/train_tiny_zero.sh` 文件：

1. **2026-01-29 23:24** - "1.5h long train"
   - 响应长度：1024 → 768
   - Tensor并行度：1 → 2
   - GPU内存利用率：0.1 → 0.2

2. **2026-01-29 23:58** - "single card bs 8 to 4"
   - 新增单卡batch size配置：`export single_gpu_size=4`
   - 验证batch size：1312 → 128
   - 响应长度：768 → 1024
   - 多种micro batch size：8 → 4
   - 新增critic mini batch size配置

3. **未提交改动**
   - 多种micro batch size：4 → 8
   - GPU内存利用率：0.2 → 0.05

## 二、8卡3090环境OOM问题分析

### 硬件配置
- **GPU**：8 × NVIDIA GeForce RTX 3090，每卡24GB显存
- **CPU**：Intel(R) Xeon(R) Silver 4310 CPU @ 2.10GHz，48核
- **内存**：充足的系统内存

### OOM问题原因
1. **Batch Size过大**：单卡micro batch size=8对于3090的24GB内存来说过大
2. **序列长度过长**：max_response_length=1024会显著增加内存消耗
3. **内存管理不当**：VLLM内存利用率设置不合理
4. **内存累积效应**：训练过程中内存逐渐增长，最终导致OOM

### 优化建议
1. **调整Batch Size**：单卡micro batch size=4
2. **减小序列长度**：max_response_length=768
3. **优化模型并行**：tensor_model_parallel_size=4
4. **合理设置内存利用率**：gpu_memory_utilization=0.15
5. **添加内存监控**：实时监控内存使用情况

## 三、训练脚本优化

### 修改的配置参数
```bash
# Batch Size 策略
export single_gpu_size=4

python3 -m verl.trainer.main_ppo \
    data.train_batch_size=128 \
    data.val_batch_size=128 \
    data.max_prompt_length=256 \
    data.max_response_length=768 \
    actor_rollout_ref.actor.ppo_micro_batch_size=4 \
    actor_rollout_ref.rollout.log_prob_micro_batch_size=4 \
    actor_rollout_ref.rollout.tensor_model_parallel_size=4 \
    actor_rollout_ref.rollout.gpu_memory_utilization=0.15 \
    actor_rollout_ref.ref.log_prob_micro_batch_size=4 \
    critic.ppo_micro_batch_size=4 \
```

### 环境配置优化
- **CUDA内存分配**：`export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`
- **禁用NCCL P2P**：`export NCCL_P2P_DISABLE=1`
- **禁用Ray内存监控**：`export RAY_memory_monitor_refresh_ms=0`

## 四、显存监控工具实现

### 创建的监控工具
**文件**：`verl/utils/debug/memory_monitor.py`

#### 核心功能
1. **`get_model_memory_usage`**：计算模型参数占用的内存
2. **`get_optimizer_memory_usage`**：计算优化器状态占用的内存
3. **`get_tensor_memory_usage`**：计算激活值等张量占用的内存
4. **`log_detailed_memory_usage`**：综合监控并记录各部分内存使用情况

#### 监控的内存部分
- **模型参数内存**：模型权重和偏置
- **优化器状态内存**：AdamW的动量和方差参数
- **激活值内存**：前向传播的中间张量
- **其他内存**：缓存、临时变量等

## 五、训练代码内存监控集成

### 修改的文件
1. **`verl/workers/actor/dp_actor.py`**：在Actor训练过程中添加内存监控
2. **`verl/workers/critic/dp_critic.py`**：在Critic训练过程中添加内存监控

### 监控的关键节点
1. **训练开始前**：初始内存状态
2. **数据加载后**：数据移至GPU后的内存状态
3. **前向传播后**：计算激活值后的内存状态
4. **反向传播后**：计算梯度后的内存状态
5. **优化器更新后**：参数更新后的内存状态
6. **训练结束后**：清理缓存后的内存状态

### Wandb集成
- 将内存监控数据添加到训练metrics中
- 在Wandb中创建内存使用趋势图表
- 监控路径：`actor/memory/*` 和 `critic/memory/*`

## 六、环境配置问题解决

### 环境问题
- 系统默认Python环境缺少`tensordict`模块
- 存在名为"zero"的conda环境，已配置使用该环境进行训练
- 优化内存使用策略，不使用param_offload以避免系统内存不足被kill
- 调整参数降低内存占用：
  - 减小max_response_length从768到512
  - 减小gpu_memory_utilization从0.15到0.1
- 修复内存监控代码集成问题，确保数据能正确上传到wandb
- 内存监控已集成到actor和critic的训练过程中，包括关键节点的内存使用情况

### 解决方案
1. **找到正确的环境路径**：`/home/ksa/.conda/envs/zero/`
2. **修改训练脚本**：指定使用zero环境的Python解释器
   ```bash
   # 使用zero环境的Python解释器
   PYTHON_EXECUTABLE="/home/ksa/.conda/envs/zero/bin/python"
   
   $PYTHON_EXECUTABLE -m verl.trainer.main_ppo \
   ```
3. **验证环境**：确认zero环境包含所有必要依赖
   - Python 3.9.25
   - tensordict 0.10.0
   - torch 2.4.0
   - transformers 4.47.1
   - vllm 0.6.3

## 七、使用方法

### 1. 设置环境变量
```bash
export DATA_DIR=/path/to/data
export BASE_MODEL=your/model/path
export N_GPUS=8  # 8卡3090
export EXPERIMENT_NAME=your_experiment_name
```

### 2. 运行训练脚本
```bash
bash scripts/train_tiny_zero.sh
```

### 3. 查看内存监控数据
- **终端日志**：详细的内存使用信息
- **Wandb Dashboard**：内存使用趋势图表
  - `actor/memory/allocated`：Actor模型分配的内存
  - `actor/memory/model`：Actor模型参数内存
  - `critic/memory/allocated`：Critic模型分配的内存
  - `critic/memory/model`：Critic模型参数内存

## 八、预期效果

1. **解决OOM问题**：通过优化batch size和序列长度，避免训练过程中内存溢出
2. **内存使用透明化**：详细的内存监控帮助识别内存瓶颈
3. **资源利用最大化**：合理的参数配置充分利用8卡3090的内存资源
4. **训练稳定性提升**：内存使用的稳定监控和优化确保训练过程稳定运行
5. **可视化监控**：通过Wandb实时查看内存使用情况和训练进度

## 九、技术亮点

1. **精细化内存监控**：区分模型参数、优化器状态、激活值等不同部分的内存使用
2. **多节点内存跟踪**：在训练的各个关键节点跟踪内存变化
3. **自动化Wandb集成**：内存监控数据自动上传到Wandb，便于可视化分析
4. **硬件适配优化**：针对8卡3090环境的专门参数优化
5. **环境配置自动化**：自动使用正确的conda环境，避免依赖问题

## 十、总结

通过对TinyZero代码的深入分析和优化，成功解决了8卡3090环境下的OOM问题，并实现了详细的显存监控功能。主要改进包括：

1. **参数优化**：调整batch size、序列长度和模型并行度，适应3090的硬件特性
2. **内存监控**：创建了专门的内存监控工具，实时跟踪各部分内存使用情况
3. **可视化集成**：将内存监控数据与Wandb集成，提供直观的内存使用趋势
4. **环境配置**：解决了conda环境配置问题，确保依赖正确安装

这些改进不仅解决了当前的OOM问题，也为后续的模型训练和优化提供了有力的工具支持。