# TinyZero 显存优化分析与方案

## 一、显存不足原因分析

在8卡3090（每卡24GB）环境下出现显存不足的主要原因：

### 1. 模型并行策略不合理
- 当前`tensor_model_parallel_size=4`，仅使用4张GPU
- 每卡承载约0.75B参数，内存占用较高
- 剩余4张GPU未充分利用，导致整体内存效率低下

### 2. VLLM配置问题
- 虽然`gpu_memory_utilization=0.1`较低，但TP=4导致部分GPU空闲
- 预分配内存可能集中在部分GPU上

### 3. 优化器内存占用
- AdamW优化器需要2倍参数大小的内存（约6GB）
- 未使用内存高效的优化器变体

## 二、显存使用情况分析

### 1. 各组件内存占用计算
| 组件 | 内存占用（3B模型） | 说明 |
|------|-------------------|------|
| 模型参数 | 约6GB | bfloat16精度 |
| 优化器状态 | 约6GB | AdamW需要2倍参数内存 |
| 激活值 | 约8-12GB | 与batch size、序列长度相关 |
| VLLM预分配 | 约2.4GB/卡 | gpu_memory_utilization=0.1 |
| 中间缓存 | 约2-4GB | 训练步数、梯度累积 |

### 2. 当前8卡配置下的内存分布
- **TP=4**：每卡承载0.75B参数，内存占用约1.5GB
- **Batch Size**：micro_batch_size=8，每卡实际batch=1
- **序列长度**：max_prompt_length=256 + max_response_length=512 = 768

## 三、显存优化方案

### 1. 模型并行策略优化
```bash
# 将TP从4调整为8，充分利用所有GPU
actor_rollout_ref.rollout.tensor_model_parallel_size=8
```
- **效果**：每卡仅需承载约0.375B参数
- **内存节省**：每卡模型参数内存降至约0.75GB（bfloat16）

### 2. Batch Size策略优化
```bash
# 保持当前合理配置
actor_rollout_ref.actor.ppo_micro_batch_size=8
actor_rollout_ref.actor.ppo_mini_batch_size=128
```
- **分析**：8卡环境下每卡实际batch=1，符合内存限制
- **优势**：梯度累积步数=16，训练稳定性良好

### 3. VLLM配置优化
```bash
# 保持低预分配，调整TP
actor_rollout_ref.rollout.gpu_memory_utilization=0.1
actor_rollout_ref.rollout.tensor_model_parallel_size=8
```
- **效果**：每卡预分配内存约2.4GB
- **优势**：配合TP=8，内存分布更均匀

### 4. 优化器选择
| 优化器 | 内存占用 | 特点 | 推荐指数 |
|--------|----------|------|----------|
| AdamW | 2×参数内存 | 稳定收敛 | ★★★☆☆ |
| AdamW8bit | 0.5×参数内存 | 内存高效 | ★★★★★ |
| Lion | 2×参数内存 | 收敛更快 | ★★★★☆ |
| SGD with momentum | 1×参数内存 | 内存最低 | ★★★☆☆ |

### 5. 序列长度优化
```bash
# 保持当前合理配置
data.max_prompt_length=256
data.max_response_length=512
```
- **分析**：总序列长度768，对3090 24GB显存来说合理
- **优势**：避免过长序列导致的激活值内存爆炸

## 四、预期效果

通过以上优化，每卡内存占用预计：

| 组件 | 内存占用（优化后） | 说明 |
|------|-------------------|------|
| 模型参数 | 约0.75GB | bfloat16，TP=8 |
| 优化器状态 | 约1.5GB | AdamW或更低（AdamW8bit） |
| 激活值 | 约4-6GB | batch=1，序列长度768 |
| VLLM预分配 | 约2.4GB | gpu_memory_utilization=0.1 |
| 其他开销 | 约2-3GB | 中间缓存、梯度等 |
| **总计** | **约10-13GB/卡** | **远低于3090的24GB限制** |

## 五、实施步骤

1. **修改模型并行配置**：
   - 更新`train_tiny_zero.sh`中的`tensor_model_parallel_size=8`

2. **调整VLLM配置**：
   - 确保`gpu_memory_utilization=0.1`
   - 配合TP=8使用

3. **选择内存高效的优化器**：
   - 推荐使用AdamW8bit减少内存占用

4. **验证训练稳定性**：
   - 运行训练脚本，观察显存使用情况
   - 检查wandb中的内存监控指标

5. **进一步优化（如需要）**：
   - 调整序列长度
   - 尝试不同的优化器

## 六、总结

通过合理的模型并行策略、Batch Size配置、VLLM优化和内存高效的优化器选择，可以显著降低每卡的内存占用，充分利用8卡3090的硬件资源，避免显存不足的问题，提高训练稳定性和效率。

### 关键优化点
- **TP=8**：充分利用所有GPU，分散模型参数
- **AdamW8bit**：减少优化器内存占用
- **合理的Batch Size**：确保每卡batch=1，避免内存爆炸
- **低VLLM预分配**：减少不必要的内存占用
- **适中的序列长度**：平衡模型性能和内存使用

这些优化措施将使TinyZero在8卡3090环境下稳定运行，充分发挥硬件性能。